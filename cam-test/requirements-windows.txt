# Windows / desktop test environment for cam-test
# This is a reduced set of packages derived from the Raspberry Pi setup.
# It excludes Debian/Raspberry Pi-only packages such as apt-listchanges, python-apt,
# gpiod, gpiozero, spidev, rpi-* packages, and ssh-import-id.

# DepthAI + node support for OAK-D
depthai==3.5.0
depthai-nodes==0.4.0

# Common runtime packages used by the demo environment
python-dotenv
opencv-python-headless==4.10.0.84
numpy==2.4.4
PyYAML==6.0.2
requests==2.32.3
